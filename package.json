{
  "name": "my_project",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "^20.10.0",
    "npm": "^10.2.0"
  },
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "cmake": "cmake-js build",
    "start": "npm run build && npm run cmake && node build/index.js"
  },
  "dependencies": {
    "cmake-js": "^7.0.0",
    "node-addon-api": "^7.0.0",
    "typescript": "^4.4.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
