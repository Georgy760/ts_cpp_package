{
  "name": "my_project",
  "version": "1.0.0",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "cmake": "cmake-js build",
    "start": "npm run build && npm run cmake && node build/index.js"
  },
  "dependencies": {
    "cmake-js": "^7.0.0",
    "node-addon-api": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10"
  }
}
